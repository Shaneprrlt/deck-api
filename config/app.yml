# config/app.yml for rails-settings-cached
defaults: &defaults
  card_load_limit: 20
  new_message_event: 'new_message'
  default_from: 'shane@deckapp.io'

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
